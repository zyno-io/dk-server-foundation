diff --git a/node_modules/@deepkit/postgres/dist/cjs/src/postgres-adapter.js b/node_modules/@deepkit/postgres/dist/cjs/src/postgres-adapter.js
index b7badb0..8151aa3 100644
--- a/node_modules/@deepkit/postgres/dist/cjs/src/postgres-adapter.js
+++ b/node_modules/@deepkit/postgres/dist/cjs/src/postgres-adapter.js
@@ -228,6 +228,21 @@ function typeSafeDefaultValue(property) {
         return false;
     return null;
 }
+function isStringEnumLikeType(type) {
+    if (type.kind === type_1.ReflectionKind.enum) {
+        return Array.isArray(type.values) && type.values.length > 0 && typeof type.values[0] === 'string';
+    }
+    if (type.kind === type_1.ReflectionKind.union) {
+        var nonNullTypes = type.types.filter(function (t) { return t.kind !== type_1.ReflectionKind.null && t.kind !== type_1.ReflectionKind.undefined; });
+        if (nonNullTypes.length === 0) return false;
+        if (nonNullTypes.length === 1) return isStringEnumLikeType(nonNullTypes[0]);
+        return nonNullTypes.every(function (t) { return t.kind === type_1.ReflectionKind.literal && typeof t.literal === 'string'; });
+    }
+    if (type.kind === type_1.ReflectionKind.intersection) {
+        return type.types.some(function (t) { return isStringEnumLikeType(t); });
+    }
+    return false;
+}
 class PostgresPersistence extends sql_1.SQLPersistence {
     constructor(platform, connectionPool, session) {
         super(platform, connectionPool, session);
@@ -257,7 +272,12 @@ class PostgresPersistence extends sql_1.SQLPersistence {
             let pkValue = entity.primaryKey.sqlTypeCast(placeholderStrategy.getPlaceholder());
             valuesValues.push('(' + pkValue + ',' + prepared.changedProperties.map(property => {
                 params.push(prepared.values[property.name][i]);
-                return property.sqlTypeCast(placeholderStrategy.getPlaceholder());
+                const placeholder = placeholderStrategy.getPlaceholder();
+                if (isStringEnumLikeType(property.type)) {
+                    const enumTypeName = entity.tableName + '_' + property.name;
+                    return placeholder + '::' + entity.platform.quoteIdentifier(enumTypeName);
+                }
+                return property.sqlTypeCast(placeholder);
             }).join(',') + ')');
         }
         for (let i = 0; i < changeSets.length; i++) {
diff --git a/node_modules/@deepkit/postgres/dist/cjs/src/postgres-platform.js b/node_modules/@deepkit/postgres/dist/cjs/src/postgres-platform.js
index 082657f..1a94d4b 100644
--- a/node_modules/@deepkit/postgres/dist/cjs/src/postgres-platform.js
+++ b/node_modules/@deepkit/postgres/dist/cjs/src/postgres-platform.js
@@ -50,6 +50,21 @@ function escapeLiteral(value) {
     }
     return escaped;
 }
+function isStringEnumLikeType(type) {
+    if (type.kind === type_1.ReflectionKind.enum) {
+        return Array.isArray(type.values) && type.values.length > 0 && typeof type.values[0] === 'string';
+    }
+    if (type.kind === type_1.ReflectionKind.union) {
+        var nonNullTypes = type.types.filter(function (t) { return t.kind !== type_1.ReflectionKind.null && t.kind !== type_1.ReflectionKind.undefined; });
+        if (nonNullTypes.length === 0) return false;
+        if (nonNullTypes.length === 1) return isStringEnumLikeType(nonNullTypes[0]);
+        return nonNullTypes.every(function (t) { return t.kind === type_1.ReflectionKind.literal && typeof t.literal === 'string'; });
+    }
+    if (type.kind === type_1.ReflectionKind.intersection) {
+        return type.types.some(function (t) { return isStringEnumLikeType(t); });
+    }
+    return false;
+}
 class PostgresPlaceholderStrategy extends sql_1.SqlPlaceholderStrategy {
     getPlaceholder() {
         return '$' + (++this.offset);
@@ -89,6 +104,9 @@ class PostgresPlatform extends sql_1.DefaultPlatform {
         if ((0, type_1.isReferenceType)(type)) {
             type = type_1.ReflectionClass.from(type).getPrimary().type;
         }
+        if (isStringEnumLikeType(type)) {
+            return (placeholder) => placeholder;
+        }
         const dbType = this.getTypeMapping(type);
         if (!dbType)
             return super.getSqlTypeCaster(type);
diff --git a/node_modules/@deepkit/postgres/dist/esm/src/postgres-adapter.js b/node_modules/@deepkit/postgres/dist/esm/src/postgres-adapter.js
index 46fca36..b7136c9 100644
--- a/node_modules/@deepkit/postgres/dist/esm/src/postgres-adapter.js
+++ b/node_modules/@deepkit/postgres/dist/esm/src/postgres-adapter.js
@@ -218,6 +218,21 @@ function typeSafeDefaultValue(property) {
         return false;
     return null;
 }
+function isStringEnumLikeType(type) {
+    if (type.kind === ReflectionKind.enum) {
+        return Array.isArray(type.values) && type.values.length > 0 && typeof type.values[0] === 'string';
+    }
+    if (type.kind === ReflectionKind.union) {
+        var nonNullTypes = type.types.filter(function (t) { return t.kind !== ReflectionKind.null && t.kind !== ReflectionKind.undefined; });
+        if (nonNullTypes.length === 0) return false;
+        if (nonNullTypes.length === 1) return isStringEnumLikeType(nonNullTypes[0]);
+        return nonNullTypes.every(function (t) { return t.kind === ReflectionKind.literal && typeof t.literal === 'string'; });
+    }
+    if (type.kind === ReflectionKind.intersection) {
+        return type.types.some(function (t) { return isStringEnumLikeType(t); });
+    }
+    return false;
+}
 export class PostgresPersistence extends SQLPersistence {
     constructor(platform, connectionPool, session) {
         super(platform, connectionPool, session);
@@ -247,7 +262,12 @@ export class PostgresPersistence extends SQLPersistence {
             let pkValue = entity.primaryKey.sqlTypeCast(placeholderStrategy.getPlaceholder());
             valuesValues.push('(' + pkValue + ',' + prepared.changedProperties.map(property => {
                 params.push(prepared.values[property.name][i]);
-                return property.sqlTypeCast(placeholderStrategy.getPlaceholder());
+                const placeholder = placeholderStrategy.getPlaceholder();
+                if (isStringEnumLikeType(property.type)) {
+                    const enumTypeName = entity.tableName + '_' + property.name;
+                    return placeholder + '::' + entity.platform.quoteIdentifier(enumTypeName);
+                }
+                return property.sqlTypeCast(placeholder);
             }).join(',') + ')');
         }
         for (let i = 0; i < changeSets.length; i++) {
diff --git a/node_modules/@deepkit/postgres/dist/esm/src/postgres-platform.js b/node_modules/@deepkit/postgres/dist/esm/src/postgres-platform.js
index 6db59dc..f5420bb 100644
--- a/node_modules/@deepkit/postgres/dist/esm/src/postgres-platform.js
+++ b/node_modules/@deepkit/postgres/dist/esm/src/postgres-platform.js
@@ -44,6 +44,21 @@ function escapeLiteral(value) {
     }
     return escaped;
 }
+function isStringEnumLikeType(type) {
+    if (type.kind === ReflectionKind.enum) {
+        return Array.isArray(type.values) && type.values.length > 0 && typeof type.values[0] === 'string';
+    }
+    if (type.kind === ReflectionKind.union) {
+        var nonNullTypes = type.types.filter(function (t) { return t.kind !== ReflectionKind.null && t.kind !== ReflectionKind.undefined; });
+        if (nonNullTypes.length === 0) return false;
+        if (nonNullTypes.length === 1) return isStringEnumLikeType(nonNullTypes[0]);
+        return nonNullTypes.every(function (t) { return t.kind === ReflectionKind.literal && typeof t.literal === 'string'; });
+    }
+    if (type.kind === ReflectionKind.intersection) {
+        return type.types.some(function (t) { return isStringEnumLikeType(t); });
+    }
+    return false;
+}
 export class PostgresPlaceholderStrategy extends SqlPlaceholderStrategy {
     getPlaceholder() {
         return '$' + (++this.offset);
@@ -82,6 +97,9 @@ export class PostgresPlatform extends DefaultPlatform {
         if (isReferenceType(type)) {
             type = ReflectionClass.from(type).getPrimary().type;
         }
+        if (isStringEnumLikeType(type)) {
+            return (placeholder) => placeholder;
+        }
         const dbType = this.getTypeMapping(type);
         if (!dbType)
             return super.getSqlTypeCaster(type);
diff --git a/node_modules/@deepkit/postgres/src/postgres-adapter.ts b/node_modules/@deepkit/postgres/src/postgres-adapter.ts
index 36bea39..2a12dcd 100644
--- a/node_modules/@deepkit/postgres/src/postgres-adapter.ts
+++ b/node_modules/@deepkit/postgres/src/postgres-adapter.ts
@@ -277,6 +277,22 @@ function typeSafeDefaultValue(property: ReflectionProperty): any {
     return null;
 }
 
+function isStringEnumLikeType(type: Type): boolean {
+    if (type.kind === ReflectionKind.enum) {
+        return Array.isArray((type as any).values) && (type as any).values.length > 0 && typeof (type as any).values[0] === 'string';
+    }
+    if (type.kind === ReflectionKind.union) {
+        const nonNullTypes = (type as any).types.filter((t: any) => t.kind !== ReflectionKind.null && t.kind !== ReflectionKind.undefined);
+        if (nonNullTypes.length === 0) return false;
+        if (nonNullTypes.length === 1) return isStringEnumLikeType(nonNullTypes[0]);
+        return nonNullTypes.every((t: any) => t.kind === ReflectionKind.literal && typeof t.literal === 'string');
+    }
+    if (type.kind === ReflectionKind.intersection) {
+        return (type as any).types.some((t: any) => isStringEnumLikeType(t));
+    }
+    return false;
+}
+
 export class PostgresPersistence extends SQLPersistence {
     constructor(protected platform: DefaultPlatform, public connectionPool: PostgresConnectionPool, session: DatabaseSession<any>) {
         super(platform, connectionPool, session);
@@ -308,7 +324,12 @@ export class PostgresPersistence extends SQLPersistence {
 
             valuesValues.push('(' + pkValue + ',' + prepared.changedProperties.map(property => {
                 params.push(prepared.values[property.name][i]);
-                return property.sqlTypeCast(placeholderStrategy.getPlaceholder());
+                const placeholder = placeholderStrategy.getPlaceholder();
+                if (isStringEnumLikeType(property.type)) {
+                    const enumTypeName = entity.tableName + '_' + property.name;
+                    return placeholder + '::' + entity.platform.quoteIdentifier(enumTypeName);
+                }
+                return property.sqlTypeCast(placeholder);
             }).join(',') + ')');
         }
 
diff --git a/node_modules/@deepkit/postgres/src/postgres-platform.ts b/node_modules/@deepkit/postgres/src/postgres-platform.ts
index cd394b0..d940746 100644
--- a/node_modules/@deepkit/postgres/src/postgres-platform.ts
+++ b/node_modules/@deepkit/postgres/src/postgres-platform.ts
@@ -70,6 +70,22 @@ function escapeLiteral(value: any): string {
     return escaped;
 }
 
+function isStringEnumLikeType(type: Type): boolean {
+    if (type.kind === ReflectionKind.enum) {
+        return Array.isArray((type as any).values) && (type as any).values.length > 0 && typeof (type as any).values[0] === 'string';
+    }
+    if (type.kind === ReflectionKind.union) {
+        const nonNullTypes = (type as any).types.filter((t: any) => t.kind !== ReflectionKind.null && t.kind !== ReflectionKind.undefined);
+        if (nonNullTypes.length === 0) return false;
+        if (nonNullTypes.length === 1) return isStringEnumLikeType(nonNullTypes[0]);
+        return nonNullTypes.every((t: any) => t.kind === ReflectionKind.literal && typeof t.literal === 'string');
+    }
+    if (type.kind === ReflectionKind.intersection) {
+        return (type as any).types.some((t: any) => isStringEnumLikeType(t));
+    }
+    return false;
+}
+
 export class PostgresPlaceholderStrategy extends SqlPlaceholderStrategy {
     override getPlaceholder() {
         return '$' + (++this.offset);
@@ -117,6 +133,13 @@ export class PostgresPlatform extends DefaultPlatform {
             type = ReflectionClass.from(type).getPrimary().type;
         }
 
+        // For string enums, skip the ::text cast. PostgreSQL enum columns
+        // reject explicit ::text values; untyped parameters resolve via
+        // assignment / comparison context instead.
+        if (isStringEnumLikeType(type)) {
+            return (placeholder: string) => placeholder;
+        }
+
         const dbType = this.getTypeMapping(type);
         if (!dbType) return super.getSqlTypeCaster(type);
 
